# Copy to `.env` in the project root and set your keys (never commit `.env`).
OPENAI_API_KEY=your_key_here
# Places API (New) — enable "Places API (New)" in Google Cloud
GOOGLE_MAPS_API_KEY=your_google_maps_key_here

# Stripe (publishable only — never put the secret key in the client bundle)
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Local Stripe Node backend (LAN IP for physical devices; see backend startup logs)
# EXPO_PUBLIC_STRIPE_API_URL=http://192.168.1.10:3000
# EXPO_PUBLIC_STRIPE_HTTP_DISABLED=1
