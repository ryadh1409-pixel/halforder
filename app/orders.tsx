/**
 * Standalone orders route (`/orders`) — same realtime hub as the tab; tab chrome omitted here.
 */
import MarketplaceOrdersScreen from '@/screens/MarketplaceOrdersScreen';

export default MarketplaceOrdersScreen;
