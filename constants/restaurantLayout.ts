/** Shared layout measurements so hero parallax stays aligned with the overlapping logo card. */
export const RestaurantHeroShell = {
  /** Total hero viewport height before the overlapped restaurant info card. */
  height: 288,
};

/** Pulls the floating info card upward over the hero (visual overlap). */
export const RESTAURANT_INFO_OVERLAP = 66;
