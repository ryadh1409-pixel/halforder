/**
 * App theme entry — design tokens live in `@/theme/theme`.
 */
export {
  Colors,
  colors,
  gradients,
  layoutStyles,
  ordersPalette,
  palette,
  radius,
  shadows,
  spacing,
  theme,
  typography,
} from '../theme/theme';
